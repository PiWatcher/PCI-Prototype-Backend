version: '3.4'

services:
  pci-backend:
    container_name: pci-backend:latest
    image: piwatcher/pci-backend:latest
    restart: unless-stopped
    environment:
      APP_ENV: Production 
      MONGO_USER: <insert_mongo_user>
      MONGO_PASS: <insert_mongo_pass> 
      MONGO_HOSTNAME: mongodb
      MONGO_PORT: 27017
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5000:5000"
    depends_on:
      - mongodb 
    networks:
      - backend

  mongodb:
    image: mongo:4.0.8
    container_name: mongodb
    restart: unless-stopped 
    command: mongod --auth
    environment:
      MONGO_INITDB_ROOT_USERNAME: <insert_root_user> 
      MONGO_INITDB_ROOT_PASSWORD: <insert_root_pass>
      MONGODB_DATA_DIR: /data/db
      MONGODB_LOG_DIR: /dev/null
    volumes:
      - mongodbdata:/data/db
    networks:
      - backend

volumes:
  mongodbdata:
    driver: local

networks:
  backend:
    driver: bridge
